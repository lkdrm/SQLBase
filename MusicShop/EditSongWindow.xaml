<ui:FluentWindow x:Class="MusicShop.EditSongWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 mc:Ignorable="d"
                 Title="Edit song" Height="450" Width="400"
                 WindowBackdropType="Mica"
                 ExtendsContentIntoTitleBar="True"
                 WindowStartupLocation="CenterOwner"
                 ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Title="Edit song" Grid.Row="0" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <ui:TextBox x:Name="TitleBox" PlaceholderText="Song Title" ClearButtonEnabled="False" Margin="0,0,0,15"/>
            <ui:TextBox x:Name="ArtistBox" PlaceholderText="Artist" ClearButtonEnabled="False" Margin="0,0,0,15"/>
            <ui:TextBox x:Name="AlbumBox" PlaceholderText="Album" ClearButtonEnabled="False" Margin="0,0,0,15"/>
            <ui:TextBox x:Name="PriceBox" PlaceholderText="Price" ClearButtonEnabled="False" Margin="0,0,0,15"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <ui:Button Content="Cancel" Appearance="Secondary" Margin="0,0,10,0" Click="CancelButton_Click"/>
            <ui:Button Content="Edit" Appearance="Primary" Click="EditButton_Click"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
